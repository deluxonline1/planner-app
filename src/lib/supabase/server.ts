/**
 * Supabase server client — zameni implementaciju kada povežeš backend.
 * Koristi se u Server Components / Route Handlers.
 */
export function createSupabaseServerClient() {
  return null as unknown;
}
